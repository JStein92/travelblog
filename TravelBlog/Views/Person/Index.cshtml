@model IEnumerable<TravelBlog.Models.Location>

<html>
    <head>
    </head>
	
    <body>
		
     <h2>This is the Experience Index</h2>

    	<table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Description)
                </th>
                <th></th>
            </tr>

            @foreach(var location in Model)
			{
			    <tr> 
                    <td>
                        @Html.DisplayFor(modelItem => location.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => location.Description)
                    </td>
                    <td>
                        @Html.ActionLink("Delete", "Delete", new { id = location.Id })
                    </td>
				     <td>
                        @Html.ActionLink("Update", "Update", new { id = location.Id })
                    </td>
                </tr>
			}
			
    	</table>

		<p>@Html.ActionLink("Create New", "Create")</p>
    </body>
</html>